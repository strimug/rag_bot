# 🍷 Умный бот-консультант в Telegram

**Telegram-бот, который знает ваше меню и винную карту наизусть.** Задаёте вопрос текстом или фото — получаете ответ, подсказку по сочетанию блюд и вин или рекомендацию что заказать. Идеально подходит для ресторанов, баров и винных магазинов.

---

## ✨ Что умеет бот

| Возможность | Описание |
|-------------|----------|
| **Ответы по меню и винам** | Бот ищет информацию в ваших документах (меню, винная карта, описания) и отвечает на вопросы: цены, состав, сочетания. |
| **Рекомендации** | Может предлагать вино к блюду, блюдо к вину и «продавать» — ненавязчиво советовать что попробовать. |
| **Память разговора** | Помнит последние сообщения: можно спросить «а к этому что подойдёт?» — бот поймёт, о чём речь. |
| **Фото** | Можно отправить фото (например, страницы меню) — бот прочитает текст и ответит или найдёт нужное в базе. |
| **Гибкость моделей** | Работает через [OpenRouter](https://openrouter.ai): можно выбрать разные модели (в том числе недорогие), один раз настроил — бот готов. |

Бот не «придумывает» цены и блюда: он опирается только на то, что вы положили в базу знаний (текстовые файлы с меню, винами и т.п.). Это и есть **RAG** — «поиск по своим документам + умный ответ».

---

## 🚀 Быстрый старт (для тех, кто не разбирается в коде)

Если вы никогда не запускали ботов — не страшно. Нужно сделать по шагам: установить Python, получить два ключа и выполнить несколько команд в терминале.

### Что понадобится

1. **Компьютер** с установленным Python 3.10 или новее.  
   Проверка: откройте терминал и введите `python3 --version`. Должна появиться версия, например `Python 3.11.x`.

2. **Токен Telegram-бота.**  
   Напишите в Telegram боту [@BotFather](https://t.me/BotFather), отправьте `/newbot`, придумайте имя и username — в ответ получите токен (длинная строка). Его нужно будет сохранить.

3. **API-ключ OpenRouter.**  
   Зарегистрируйтесь на [openrouter.ai](https://openrouter.ai), создайте API-ключ и положите на счёт немного кредитов (хватает надолго при использовании недорогих моделей).

### Шаг 1: Скачать проект и установить зависимости

В терминале (в папке, где вам удобно хранить проекты):

```bash
# Перейти в папку бота (подставьте свой путь)
cd rag_bot/bot_proxy

# Создать виртуальное окружение (рекомендуется)
python3 -m venv venv

# Активировать его
# На macOS/Linux:
source venv/bin/activate
# На Windows:
# venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 2: Настроить ключи

В папке `bot_proxy` создайте файл с именем `.env` (точка в начале важна). Внутри — две строки (подставьте свои значения):

```env
TELEGRAM_TOKEN=ваш_токен_от_BotFather
OPENROUTER_API_KEY=ваш_ключ_с_openrouter_ai
```

Файл `.env` никому не показывайте и не загружайте в интернет — в нём секреты.

### Шаг 3: Положить свои документы (меню, вина)

Текстовые файлы с меню и винной картой нужно положить в папку с документами проекта. Обычно это:

```
rag_bot/data/docs/
```

Туда кладут файлы `.txt` в кодировке UTF-8: например `menu.txt`, `wine_list.txt`. Формат — обычный текст: названия блюд, цены, описания, вина и т.д. Чем аккуратнее структура, тем лучше бот будет находить ответы.

### Шаг 4: Запустить бота

В терминале (в папке `bot_proxy`, с активированным `venv`):

```bash
python bot.py
```

В консоли должно появиться что-то вроде «RAG Pipeline готов к работе». Значит, бот запущен.

### Шаг 5: Первый диалог в Telegram

1. Найдите своего бота в Telegram по username.
2. Нажмите **Start** или отправьте `/start`.
3. Отправьте команду **`/ingest`** — бот проиндексирует все документы из `data/docs/`. Это нужно делать один раз после добавления новых файлов или изменения меню.
4. После этого можно писать обычные сообщения: «Какое вино подойдёт к стейку?», «Что есть в меню до 500 рублей?», «Расскажи про это вино» и т.д.

На этом быстрый старт заканчивается: бот уже работает как консультант по вашим меню и винам.

---

## 📱 Команды в боте

| Команда | Для чего |
|---------|----------|
| `/start` | Приветствие и краткая подсказка. |
| `/help` | Подробная справка по командам и возможностям. |
| `/ingest` | Пересобрать базу знаний (выполнять после изменения файлов в `data/docs/`). |
| `/stats` | Показать статистику: сколько документов в базе, какая модель используется и т.п. |
| `/clear` | Очистить историю разговора с ботом (начать тему заново). |

Обычные текстовые сообщения бот воспринимает как вопросы: сам ищет по базе и отвечает. Фото тоже можно отправлять — с подписью или без; бот постарается прочитать текст и связать его с меню/винами.

---

## 📂 Где что лежит

- **`bot.py`** — главный файл: запуск бота, команды, обработка сообщений и фото.
- **`config.py`** — настройки: модели, промпт «сотрудника ресторана», пути к индексу и документам. Здесь можно поменять тон бота и лимиты.
- **`rag/`** — логика поиска по документам (эмбеддинги, FAISS, RAG-пайплайн). Для «просто пользователя» трогать не обязательно.
- **`data/docs/`** (обычно на уровень выше) — сюда кладут `.txt` с меню и винами.
- После первого `/ingest` в папке бота появятся `index.faiss` и `metadata.json` — это база для быстрого поиска; их можно не открывать.

---

## 🔧 Настройка под себя (по желанию)

В `config.py` можно без программирования поменять:

- **Модели** — через переменные окружения или прямо в коде: какая модель для чата, для картинок, для эмбеддингов (по умолчанию используются недорогие варианты через OpenRouter).
- **Поведение бота** — блок `SYSTEM_PROMPT`: там описан «характер» бота (работник ресторана, советы по винам, мягкие продажи). Можете отредактировать текст под свой стиль.
- **Объём контекста** — сколько документов подтягивать к каждому вопросу (`TOP_K_RESULTS`) и максимальная длина контекста (`MAX_CONTEXT_LENGTH`).

Перезапуск бота после изменений: остановить (Ctrl+C в терминале) и снова выполнить `python bot.py`.

---

## ❓ Частые вопросы

**Бот не отвечает / пишет, что база не загружена.**  
Выполните в чате `/ingest` и дождитесь сообщения об успешной индексации. Убедитесь, что в `data/docs/` есть хотя бы один `.txt` файл.

**Ошибка про TELEGRAM_TOKEN или OPENROUTER_API_KEY.**  
Проверьте файл `.env` в папке `bot_proxy`: в нём должны быть именно такие имена переменных и ваши реальные ключи (без кавычек и пробелов вокруг `=`).

**Бот «выдумывает» блюда или цены.**  
Он отвечает только по тем документам, что в `data/docs/`. Если чего-то там нет — так и скажет (если промпт настроен честно). Проверьте, что нужная информация есть в `.txt` и после изменений вызвали `/ingest`.

**Хочу другую модель (дешевле/дороже).**  
В OpenRouter можно выбрать другую модель. В `config.py` задаётся, например, `CHAT_MODEL` — можно подставить идентификатор модели с [openrouter.ai](https://openrouter.ai/docs#models).

---

## 📄 Лицензия и использование

Проект можно использовать для своих ресторанов, баров, винных магазинов или как основу для обучения. При использовании OpenRouter и Telegram соблюдайте их условия использования и не храните секреты в открытом доступе (файл `.env` не загружайте в репозиторий).

---

**Итог:** это готовый «умный консультант» для меню и вин в Telegram. Настроил ключи и документы — запустил — общаешься. Подходит и для несведущих в программировании пользователей, если следовать шагам выше.
